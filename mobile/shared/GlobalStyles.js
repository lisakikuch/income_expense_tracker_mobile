import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  headerTitle: {
    fontWeight: "bold",
    fontSize: 20,
    color: "#333",
  },
  headerRightButton: {
    marginRight: 15,
  },
  headerRightText: {
    color: "#007AFF",
    fontSize: 16,
  },
  headerContainer: {
    backgroundColor: "#fff",
    elevation: 0,
    shadowOpacity: 0,
  },
});

export default globalStyles;